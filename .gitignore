# 유효한 JWT가 들어 있다. 절대 커밋하지 않는다. seed.py 로 다시 만들면 된다.
seed/tokens.json

# 측정 결과와 서버 로그
out/

# k6 바이너리 (약 60MB). README 에 받는 법이 있다.
bin/

# TTS 로 생성하는 오디오 픽스처. fixtures/make_audio.ps1 로 다시 만들면 된다.
fixtures/*.webm
fixtures/*.wav
fixtures/*.jpg
